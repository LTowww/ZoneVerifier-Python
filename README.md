# ZoneVerifier-Python
Python version of ZoneVerifier: identifies product regions in Brazil based on code or detects imported items. | Versão em Python do ZoneVerifier que identifica a região de produtos no Brasil ou se são importados.
